"""Knowledge-base subsystem: provider seam, document parser/chunker, and vector store.

Relocated from the top-level ``utils/`` package (where it was mislabeled as generic
utilities) into ``core`` alongside core.content.kb_processing and core.services.kb_service.
"""
